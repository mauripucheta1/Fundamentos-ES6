const autenticado = true;

// Estructura IF / ELSE
if (autenticado) {
    console.log('Acceso permitido.');
} else {
    console.log('Acceso denegado.');
};

// Operador ternario
console.log(autenticado ? 'Estás autenticado' : 'No estás autenticado');