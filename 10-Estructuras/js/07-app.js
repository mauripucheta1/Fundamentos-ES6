const usuario2 = true;
const puedePagar2 = false;

if (usuario2 || puedePagar2) {
    console.log('No, no puedes comprar')
} else {
    console.log('Si, puedes comprar');
};

// Aclaraciones: Acá vimos lo que sería el operador OR (||) de disyunción que evalúa que, por lo menos una de las X opciones se cumpla.