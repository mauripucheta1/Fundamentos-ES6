const usuario = true;
const puedePagar = true;

if (usuario && puedePagar) {
    console.log('Si puedes comprar')
} else {
    console.log('No, no puedes comprar');
};

// Aclaraciones: Acá vimos lo que sería el operador AND (&&) de conjunción que evalúa que dos operaciones o más se cumplan.