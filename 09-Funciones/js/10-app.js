// Arrow functions (función flecha)
const despedir = () => 'Estás despedido.';

despedir();

// Aclaraciones: En funciones flecha, se da por implícito el return y las llaves no son obligatorias cuando tienes una sola línea