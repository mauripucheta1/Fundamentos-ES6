const productoTrece = {

    nombre: 'Cable HDMI',
    precio: 6500,
    disponible: true,
    

};  

console.log('Objeto trece:', productoTrece);

// Método para que nos retorne un array con las llaves (keys) de un objeto
console.log('Keys del objeto "productoTrece":', Object.keys(productoTrece));

// Método para que nos retorne un array con los valores (values) de un objeto
console.log('Values del objeto "productoTrece":', Object.values(productoTrece));

// Método para que nos retorne toda la información de un objeto en pares
console.log('Entries del objeto "productoTrece":', Object.entries(productoTrece));