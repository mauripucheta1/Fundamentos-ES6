const palabraVolumen = 'Esta frase será fORmaTeADa';

// Método para transformar el string en minúscula
console.log('Transformando a minúscula:', palabraVolumen.toLowerCase());

// Método para transformar el string en mayúscula
console.log('Transformando a mayúscula:', palabraVolumen.toUpperCase());

// Método para transformar un número a string
const precio = 300;
console.log('Precio original:', precio);
console.log('Transformando precio a string:', precio.toString());